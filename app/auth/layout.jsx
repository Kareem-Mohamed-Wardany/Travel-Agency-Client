export const metadata = {
  title: "Travel Authentication",
  description: "Authentication for Travel Agency Website",
};

export default function RootLayout({ children }) {
  return <>{children}</>;
}
